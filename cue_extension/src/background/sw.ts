// MV3 requires a service-worker declaration even when the extension does no
// background work. The popup handles all orchestration; the content script
// handles all in-page activity. This worker is intentionally empty.

export {};
